// track.hpp
// Represents a single tracked object: holds IMM filter state, track ID,
// classification scores, hit/miss history, and last-seen timestamp.

#pragma once

namespace cuas {

class Track {
public:
    Track() = default;
};

} // namespace cuas
