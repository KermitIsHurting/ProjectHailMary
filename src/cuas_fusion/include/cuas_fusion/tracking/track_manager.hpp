// track_manager.hpp
// Manages the lifecycle of all active tracks: creation from new detections,
// update via IMM filter, tentative/confirmed state transitions, and deletion.

#pragma once

namespace cuas {

class TrackManager {
public:
    TrackManager() = default;
};

} // namespace cuas
